import { ConvierteInputDirective } from './convierte-input.directive';

describe('ConvierteInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvierteInputDirective();
    expect(directive).toBeTruthy();
  });
});
