import { DecoraEtiquetaDirective } from './decora-etiqueta.directive';

describe('DecoraEtiquetaDirective', () => {
  it('should create an instance', () => {
    const directive = new DecoraEtiquetaDirective();
    expect(directive).toBeTruthy();
  });
});
